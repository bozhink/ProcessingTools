@model ProcessingTools.Web.Models.Tools.Decode.DecodeBase64ViewModel

@{
    ViewData[ContextKeys.Title] = "Decode Base64 String";
    if (string.IsNullOrWhiteSpace(Model?.PageHeading))
    {
        string title = Html.DisplayNameFor(m => m.PageHeading);
        if (!string.IsNullOrWhiteSpace(title))
        {
            ViewData[ContextKeys.Title] = title;
        }
    }
    else
    {
        ViewData[ContextKeys.Title] = Model.PageHeading;
    }
}

<section role="main" class="container">
    <header>
        <h2>@ViewData[ContextKeys.Title]</h2>
    </header>
    <div class="row">
        <div class="col-md-6">
            <fieldset>
                <legend>Content</legend>
                <form method="post" class="container">
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, string.Empty, new { @class = "text-danger" })
                    @Html.HiddenFor(m => m.ReturnUrl)
                    <div class="form-group row">
                        <div class="col-md-12">
                            <textarea type="text" asp-for="Content" cols="30" rows="4" placeholder="Text to encode" class="form-control"></textarea>
                            <span asp-validation-for="Content" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-group row">
                        <div class="col-md-12">
                            <input type="submit" value="Submit" class="btn btn-outline-secondary" />
                            <input type="reset" value="Reset" class="btn btn-outline-secondary" />
                            <a asp-area="@(AreaNames.Tools)" asp-controller="@(DecodeController.ControllerName)" asp-action="@(DecodeController.Base64ActionName)" asp-route-returnUrl="@(Model.ReturnUrl)" class="btn btn-outline-secondary">Clear</a>
                        </div>
                    </div>
                </form>
            </fieldset>
        </div>
        @if (Model != null && !string.IsNullOrEmpty(Model.Base64DecodedString))
        {
            <div class="col-md-6">
                <fieldset>
                    <legend>Decoded String</legend>
                    <div>
                        <code class="hard-word-wrap">@Model.Base64DecodedString</code>
                    </div>
                </fieldset>
            </div>
        }
    </div>
    <footer class="page-navigation">
        <p><a asp-action="@(ActionNames.Index)" asp-controller="@(ControllerNames.Home)" asp-route-returnUrl="@(Model.ReturnUrl)">Back to List of Tools</a></p>
    </footer>
</section>

@section Scripts {
    <partial name="@ViewNames.ValidationScriptsPartial" />
}
